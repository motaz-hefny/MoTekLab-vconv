# vconv Requirements
# Python 3.8+

# GUI Framework
PyQt6>=6.5.0

# Help browser (markdown to HTML rendering)
markdown>=3.4.0

# Data handling
# (Using stdlib: json, pathlib, dataclasses)

# System utilities
# (Using stdlib: subprocess, shutil, threading, os, re, select, copy)
